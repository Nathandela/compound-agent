# Agent Instructions

**All agent instructions live in `.claude/CLAUDE.md`** - this file is always loaded by Claude Code.

```
.claude/
├── CLAUDE.md              <- Primary instructions (auto-loaded)
├── agents/                <- Verification subagents
└── commands/              <- Custom commands
```

For quick reference, see `.claude/CLAUDE.md`.
